apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vm-modules-pvc
spec:
  storageClassName: manual
  volumeName: vm-modules-pv4
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
